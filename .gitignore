# Secrets and local environment
.env.local
.venv/
__pycache__/
*.pyc

# The course's own scripts: copy them here to use them, but they are course
# material, so never commit or publish them.
run_eval.py
estimate_tokens.py

# Files generated by the eval steps
eval-config*.json
results*.md
